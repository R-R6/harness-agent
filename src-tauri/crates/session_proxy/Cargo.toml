[package]
name = "session_proxy"
version = "0.2.0"
description = "会话数据代理：内置 agent-sessions-mcp (server.js) 作为会话数据源"
edition = "2021"

[dependencies]
path_util = { path = "../path_util" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
